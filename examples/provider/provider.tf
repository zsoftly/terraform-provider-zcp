provider "zcp" {
  # bearer_token    = var.zcp_bearer_token  # or set ZCP_BEARER_TOKEN (recommended)
  # api_url         = "https://api.zcp.zsoftly.ca/api"  # or set ZCP_API_URL; this is the default
  # default_project = "default"             # or set ZCP_PROJECT
}
